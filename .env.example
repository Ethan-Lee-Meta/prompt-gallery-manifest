# Prompt Gallery API 环境变量配置示例

# ========================================
# 数据存储配置（可选）
# ========================================
# 默认情况下，数据存储在用户目录：
# - Windows: %LOCALAPPDATA%\prompt-gallery-data
# - Linux/Mac: ~/.local/share/prompt-gallery
#
# 如需自定义路径，取消以下注释并修改：

# 数据库路径
# DATABASE_URL=sqlite:///D:/my-data/prompt-gallery-app.db

# 媒体文件存储路径
# STORAGE_ROOT=D:/my-data/prompt-gallery-storage

# ========================================
# 应用配置
# ========================================

# 应用名称
APP_NAME=prompt-gallery-api

# 允许的跨域来源（开发环境使用 * ，生产环境指定具体域名）
ALLOW_ORIGINS=*

# 开发环境下自动创建数据库表（生产环境建议使用 Alembic）
AUTO_CREATE_TABLES=true

# ========================================
# 自动分类配置（可选）
# ========================================

# 分类阈值
AUTO_CAT_THRESHOLD=0.32

# Top-K 分类数
AUTO_CAT_TOPK=3

# 每个分类最少样本数
AUTO_CAT_MIN_SAMPLES_PER_CAT=3

# 每个分类采样数
AUTO_CAT_SAMPLE_PER_CAT=200

# 人脸增强权重
AUTO_CAT_FACE_BOOST=0.07
AUTO_CAT_FACE_NEAR_BAND=0.10

# 文本增强权重
AUTO_CAT_TEXT_BOOST=0.03
AUTO_CAT_TEXT_NEAR_BAND=0.12

# 关键词配置
AUTO_CAT_FACE_KEYWORDS=肖像,角色,人物,人像
AUTO_CAT_PERSON_TEXT_KEYWORDS=人像,肖像,人物,角色,写真,脸,面部,眼妆,唇,妆容,证件照,portrait,face,headshot,beauty,model,character,close-up,closeup,macro portrait
